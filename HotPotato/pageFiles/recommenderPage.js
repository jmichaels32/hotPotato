import { Text, View } from 'react-native';

// Local file import
import * as Styles from '../styles.js';

const RecommenderPage = () => {
	return (
		<View style={Styles.pageStyles.container}>
			<Text> This is the recommender </Text>
		</View>
	)
};

export default RecommenderPage;